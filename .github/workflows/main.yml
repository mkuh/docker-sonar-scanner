name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: get soanar files
      run: |
       git clone https://github.com/SonarSource/sonar-scanner-cli.git
       cd sonar-scanner-cli
       LAST_VERSION=$(git describe --tags `git rev-list --tags --max-count=1`)
       echo "AAAA ${LAST_VERSION}"
    - name: Create local changes
      run: |
       echo "BBB ${LAST_VERSION}"
       ls -la ..
#       sed -i 's/ENV SONAR_SCANNER_VERSION.*/ENV SONAR_SCANNER_VERSION AAA/' ./Dockerfile
    - name: Commit files
      run: |
#        git config --local user.email "action@github.com"
#        git config --local user.name "GitHub Action"
#        git commit -m "Add changes" -a
# ${{ secrets.GITHUB_TOKEN }}
